message DeviceInfo {
  enum DeviceType {
    BLUETOOTH = 0;
    WIFI = 1;
  }
  enum DeviceAbility {
    GPIO = 0;
  }
  required string name = 1;
  required DeviceType type = 2;
  required DeviceAbility ability = 3;
  optional string ip = 4;
  optional string mac = 5;
}